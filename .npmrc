# Puppeteer 下载镜像配置说明
# 
# 如果遇到 Puppeteer 下载 Chrome 失败，请使用以下方式之一：
#
# 方式1: 使用安装脚本（推荐）
#   pnpm run install:puppeteer
#
# 方式2: 在安装前设置环境变量
#   Windows PowerShell:
#     $env:PUPPETEER_DOWNLOAD_BASE_URL="https://npmmirror.com/mirrors"
#     pnpm install
#   Linux/Mac:
#     export PUPPETEER_DOWNLOAD_BASE_URL="https://npmmirror.com/mirrors"
#     pnpm install
#
# 方式3: 在安装命令中直接设置（Windows PowerShell）
#   $env:PUPPETEER_DOWNLOAD_BASE_URL="https://npmmirror.com/mirrors"; pnpm install
#
# 方式4: 跳过 Puppeteer 浏览器下载（如果已有 Chrome）
#   $env:PUPPETEER_SKIP_DOWNLOAD="true"
#   pnpm install
